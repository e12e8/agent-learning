# planner.py

def plan(task: str) -> list[str]:
    """
    把一个任务拆解成多个步骤
    """
    print("Planner 正在拆解任务...")

    steps = [
        "分析任务目标",
        "查找相关信息",
        "整理答案结构",
        "生成最终结果"
    ]

    return steps
